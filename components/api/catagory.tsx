export const CategoryApia = () => {
  // const { data, isLoading } = useCategoriesQuery({});
  // console.log(data);
  // const dispatch = useDispatch();
  // dispatch(setProfile(data));

  // if (isLoading) {
  //   return <div>Loading...</div>;
  // }
  return <div></div>;
};
